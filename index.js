
    console.log(`${bot.user.username} is online on ${bot.guilds.size} servers! example                                                                                                                                                                                                
 `);
    bot.user.setActivity("everything", {type: "LISTENING"});
  });

